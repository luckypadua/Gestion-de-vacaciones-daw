# DAW state (per-repo — never committed when installed in a project)
*.pyc
.daw-paused/
.daw-sessions/
.daw-state.json
__pycache__/

# BEGIN DAW — pipeline runtime, NOT committed (managed by DAW)
#   .daw-state.json  = which phase you are in; yours, not the repo's
#   .daw-paused/     = paused tickets
#   .daw-sessions/   = live session markers
#   .daw-journal.jsonl = transitions that landed; outlives the state file
.daw-state.json
.daw-paused/
.daw-sessions/
.daw-journal.jsonl
# END DAW

# .NET / GestionVacaciones (FEAT-001a)
bin/
obj/
*.user
.vs/
TestResults/
artifacts/
*.mdf
*.ldf
# Configuración local con credenciales: nunca se versiona (R-02)
appsettings.*.local.json

# Ruido de Visual Studio: instrucciones de Copilot que la IDE regenera dentro de src/
src/.github/
